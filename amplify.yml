version: 1
applications:
  - appRoot: frontend
    frontend:
      runtime-versions:
        nodejs: 18
      phases:
        preBuild:
          commands:
            - node -v || true
            - npm -v || true
            - npm ci || npm install --no-audit --no-fund
        build:
          commands:
            - npm run build
      artifacts:
        baseDirectory: .next
        files:
          - '**/*'
      cache:
        paths:
          - node_modules/**/*
          - .next/cache/**/*

